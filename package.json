{
  "name": "page-check",
  "version": "0.0.1",
  "description": "A server side tool that checks webpages for visual changes and - if content changed - notifies subscribed users.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && node ."
  },
  "author": "Florian Loch",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0",
    "mongoose": "^4.6.4",
    "pug": "^2.0.0-beta6"
  }
}
